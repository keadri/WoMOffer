/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/WoMOffer',
  output: 'export',
  images: {
    unoptimized: true,
  },
  assetPrefix: '/WoMOffer/',
}

module.exports = nextConfig 